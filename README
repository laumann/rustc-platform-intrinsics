librustc_platform_intrinsics
============================

Structure of this package

 * platform-intrinsics contains all the logic for parsing the JSON input and
   converting it to output (somewhere)

 * The 'lib.rs' is the dynamically linkable library that links all files
   generated by 'build.rs'

 * The 'main.rs' program allows specifying particular input files (providing the
   same command-line interface as the old Python version)

 * The 'arch' folder contains all the 
